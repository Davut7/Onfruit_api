export enum GetProductsOrderEnum {
  currentSum = 'currentSum',
  currentSpoiledQuantity = 'currentSpoiledQuantity',
  currentQuantity = 'currentQuantity',
  currentSaleQuantity = 'currentSaleQuantity',
  middlePrice = 'middlePrice',
  createdAt = 'createdAt',
}
